name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test (macOS 14, Swift 5.10)
    runs-on: macos-14
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        # Xcode 15.4 ships Swift 5.10 — matches Package.swift swift-tools 5.9
        # minimum and the MrKai77/DynamicNotchKit macro plugin requirements.
        run: sudo xcode-select -s /Applications/Xcode_15.4.app

      - name: Print toolchain
        run: |
          swift --version
          xcodebuild -version

      - name: Cache SwiftPM
        uses: actions/cache@v4
        with:
          path: |
            .build
            ~/Library/Developer/Xcode/DerivedData
            ~/Library/Caches/org.swift.swiftpm
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      - name: Resolve dependencies
        run: swift package resolve

      - name: Build (debug)
        run: swift build -v

      - name: Test
        run: swift test -v --parallel

  lint:
    name: Lint (swift-format)
    runs-on: macos-14
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: swift-format lint
        # swift-format ships with Xcode 15.4. `--strict` exits non-zero on
        # any finding, `--recursive` walks the Sources tree.
        run: |
          if command -v swift-format >/dev/null 2>&1; then
            swift-format lint --recursive --strict Sources Tests || \
              echo "::warning::swift-format reported issues (non-blocking)"
          else
            echo "::warning::swift-format not installed, skipping"
          fi
